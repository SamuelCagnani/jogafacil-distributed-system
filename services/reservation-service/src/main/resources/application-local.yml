# Perfil local: usa o LocalStack no lugar da AWS.
# Suba a infraestrutura com: docker compose -f infra/docker/docker-compose.yml up -d
aws:
  dynamodb:
    endpoint: http://localhost:4566
  access-key-id: test
  secret-access-key: test
